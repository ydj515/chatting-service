# 어드민 API 기본 base-url. 비우면 상대경로 '/api'로 폴백한다.
# 빌드 타임에 주입되며, 다른 호스트의 게이트웨이를 가리키려면 전체 URL을 넣는다.
VITE_ADMIN_DEFAULT_BASE_URL=
VITE_ADMIN_API_TIMEOUT_MS=30000
# vite dev 서버가 /api 요청을 프록시할 백엔드 게이트웨이 주소
VITE_DEV_PROXY_TARGET=http://localhost:80
