# Local development pg_hba for primary + streaming replica.
local   all             all                                     trust
host    all             all             all                     scram-sha-256
host    replication     all             all                     scram-sha-256
